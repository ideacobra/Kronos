# Additive dependencies for memecoin_bot -- kept separate from the root
# requirements.txt so people who just want the Kronos forecasting model
# aren't forced into these (and vice versa: memecoin_bot's core scan/paper
# trade/post loop does NOT need torch/pandas/numpy at all).
#
# Install with:
#   pip install -r requirements-bot.txt
#
# Only needed IN ADDITION to this if you want the optional Kronos
# confirmation signal (USE_KRONOS_SIGNAL=true in .env):
#   pip install -r requirements.txt

requests>=2.31.0
python-dotenv>=1.0.0
tweepy>=4.14.0
pytest>=7.4.0
responses>=0.24.0

# Solana wallet + live trading (`wallet new` / `wallet balance` / `run
# --live`). Paper trading (the default) does not make any network calls
# using these, but they're required to import memecoin_bot.trading.wallet /
# live_broker, so they're installed unconditionally for simplicity.
solders>=0.20.0
base58>=2.1.1
